server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Single-page-app fallback: unknown paths serve index.html so Vue Router
  # can handle client-side routes (e.g. /adopt, /pet/:id) on direct load.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
