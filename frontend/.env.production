# Frontend production environment (loaded by Vite at `build` time).
# In Docker these are supplied as build args (see docker-compose.yml).
VITE_API_BASE_URL=http://localhost:5001
VITE_WS_URL=http://localhost:5001
