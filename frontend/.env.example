# Copy to .env.development (dev) / .env.production (build) and adjust as needed.
# Base URL of the PawHub backend REST API.
VITE_API_BASE_URL=http://localhost:5001
# Base URL of the backend Socket.IO server (usually same as the API).
VITE_WS_URL=http://localhost:5001
